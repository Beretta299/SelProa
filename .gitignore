# ── secrets ──────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# ── python ───────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
dist/
*.egg-info/

# ── node ─────────────────────────────────
node_modules/
.vite/
build/
*.tsbuildinfo

# ── kotlin / gradle ──────────────────────
.gradle/
build/
!src/**/build/
local.properties

# ── terraform ────────────────────────────
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform.lock.hcl

# ── data and artefacts ───────────────────
*.db
*.sqlite3
data/
corpus/
photos/
*.log

# ── editors / os ─────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

# ── seed ground truth ────────────────────
# Regenerate with `./gradlew seed` (deterministic on SEED in Seed.kt).
# Kept out of the repo so it can never be ingested by pitwall-api or end up in
# a retrieval corpus. Do not open it until chapter 25.
registry-api/seed-truth.json
